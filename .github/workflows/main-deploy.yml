name: Deploy Expo Web to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 4️⃣ Preflight: check SettingsSheet.tsx
      - name: Check for SettingsSheet.tsx
        run: |
          echo "Checking components folder..."
          ls -R ./components || true
          if [ ! -f ./components/SettingsSheet.tsx ]; then
            echo "❌ ERROR: components/SettingsSheet.tsx not found."
            exit 1
          fi

      # 5️⃣ Build Expo for all platforms
      - name: Build Expo (iOS, Android, Web)
        run: npx expo export

      # 6️⃣ Clean old docs folder
      - name: Clean old docs folder
        run: rm -rf docs

      # 7️⃣ Ensure web build exists
      - name: Check web build folder
        run: |
          if [ ! -d "_expo/web" ]; then
            echo "❌ ERROR: _expo/web folder not found. Export may have failed."
            echo "Contents of _expo folder:"
            ls -R _expo || true
            exit 1
          fi

      # 8️⃣ Copy web build to docs
      - name: Copy web build to docs
        run: |
          mkdir -p docs
          cp -r _expo/web/* docs/

      # 9️⃣ Fix absolute paths for GitHub Pages
      - name: Fix paths for GitHub Pages
        run: |
          sed -i 's|href="/|href="./|g' docs/index.html
          sed -i 's|src="/|src="./|g' docs/index.html

      # 🔟 SPA support: copy index.html to 404.html
      - name: SPA support for GitHub Pages
        run: cp docs/index.html docs/404.html

      # 1️⃣1️⃣ Commit and push changes
      - name: Commit and push changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs
          git commit -m "Deploy web build to docs [skip ci]" || echo "No changes to commit"
          git push
