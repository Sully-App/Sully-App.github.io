name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - run: npm install

      # 4️⃣ Preflight: ensure SettingsSheet exists
      - run: |
          echo "Checking components folder..."
          ls -R ./components || true
          if [ ! -f ./components/SettingsSheet.tsx ]; then
            echo "❌ ERROR: components/SettingsSheet.tsx not found. Check casing/path."
            exit 1
          fi

      # 5️⃣ Build web
      - name: Build Expo Web
        run: npx expo build:web

      # 6️⃣ Clean old docs
      - run: |
          rm -rf docs
          rm -rf _expo/web || true

      # 7️⃣ Copy web build to docs
      - run: |
          mkdir -p docs
          cp -r web-build/* docs/

      # 8️⃣ Fix paths for GitHub Pages
      - run: |
          sed -i 's|href="/|href="./|g' docs/index.html
          sed -i 's|src="/|src="./|g' docs/index.html

      # 9️⃣ SPA support
      - run: cp docs/index.html docs/404.html

      # 🔟 Commit & push
      - run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs
          git commit -m "Deploy web build to docs [skip ci]" || echo "No changes to commit"
          git push
