name: Deploy Expo Web to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 4️⃣ Preflight: ensure SettingsSheet.tsx exists
      - name: Check for SettingsSheet.tsx
        run: |
          echo "Checking components folder..."
          ls -R ./components || true
          if [ ! -f ./components/SettingsSheet.tsx ]; then
            echo "❌ ERROR: components/SettingsSheet.tsx not found. Check casing/path."
            exit 1
          fi

      # 5️⃣ Export web assets only
      - name: Export Expo Web
        run: npx expo export --platform web

      # 6️⃣ Verify export
      - name: Verify web export
        run: |
          if [ ! -d "_expo/web" ]; then
            echo "❌ ERROR: _expo/web folder not found. Export may have failed."
            echo "Contents of _expo (if any):"
            ls -R _expo || true
            exit 1
          fi

      # 7️⃣ Clean old docs folder
      - name: Clean old docs folder
        run: |
          rm -rf docs

      # 8️⃣ Copy web export to docs
      - name: Copy web export to docs
        run: |
          mkdir -p docs
          cp -r _expo/web/* docs/

      # 9️⃣ Fix absolute paths for GitHub Pages
      - name: Fix paths for GitHub Pages
        run: |
          sed -i 's|href="/|href="./|g' docs/index.html
          sed -i 's|src="/|src="./|g' docs/index.html

      # 🔟 SPA support: copy index.html to 404.html
      - name: GitHub Pages SPA fix
        run: cp docs/index.html docs/404.html

      # 1️⃣1️⃣ Commit and push changes
      - name: Commit and push docs
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs
          git commit -m "Deploy Expo Web to docs [skip ci]" || echo "No changes to commit"
          git push
